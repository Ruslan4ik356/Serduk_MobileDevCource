import { StyleSheet } from "react-native";

export const headerStyles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    padding: 16,
    alignItems: "center",
  },
  title: {
    fontSize: 20,
    fontWeight: "bold",
  },
});
